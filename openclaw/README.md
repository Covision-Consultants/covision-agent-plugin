# Covision for OpenClaw

This directory is the ClawHub publication wrapper for the portable Covision Agent Plugins bundle at the repository root. It intentionally contains no native runtime code. It supplies the same read-only skill and Streamable HTTP MCP connection.
